
<!DOCTYPE html>
<html>
  <head>
    <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x16.png">
    <link rel="manifest" href="/images/site.webmanifest">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Hoc Momento - Spectacles</title>
  </head>
  <body>
    <%- include('./partials/navbar') %> 
      <div class="relative">
        <div class="shows-banner-photo"></div>
        <div class="banner">
          <h1 class="banner-title white">SPECTACLES</h1>
          <div class="arroow down">
            <div class="arrow-top"></div>
            <div class="arrow-bottom"></div>
          </div>
        </div>

        <div class="aactions-text">
          <!-- <p class="aactions-desc">
            DESCRIPTION DES SPECTACLES
          </p> -->
        </div>

    <div class="made-container aactions-container">
        <% for (i=0; i< shows.length; i++) {%>
          <a href="/spectacle?id=<%=shows[i].id %>" class="white link-white aactions-img">
            <img src="<%= shows[i].photo %>" class="big-img" alt="Hoc Momento et <%= shows[i].title %> "/>
            <div class="shows-hover">
              <p class="shows-title"><%= shows[i].title %></p>
              <% for (let j=0; j< shows[i].place.length; j++) {%>
                <div class="shows-period">
                 <%= shows[i].place[j] %> <br/>
                  <%= shows[i].city[j] %><br/> 
                  <%= shows[i].period[j] %>
              </div>
              <% } %>  
            </div>
          </a>
        <% } %> 
    </div>
  </div>
  <%- include('./partials/footer'); %> 
  </body>
</html>